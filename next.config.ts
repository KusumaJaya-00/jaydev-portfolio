import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Halaman publik kini langsung di app/ — tidak perlu rewrite lagi.
     Biarkan kosong; struktur: app/(home, projects, blog, contact), app/admin, app/login */
};

export default nextConfig;
